/**
 * Deterministic synthetic demo fixtures land in later phases.
 *
 * This package holds the committed synthetic identities, hero presets, and
 * signal payloads used by the demo. Everything here is synthetic by design
 * and tagged as such (docs/EXECUTION.md PART 3, contract rule: synthetic
 * provider data is tagged as synthetic in storage and in the UI).
 */
export {};
